{
  "name": "parcel-gh-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "tape 'test/**/*-test.js' | tap-spec",
    "start": "parcel src/index.html aubio.wasm --out-dir dev",
    "build": "parcel build src/index.html aubio.wasm --out-dir docs --public-url ./",
    "deploy": "npm run build && git add docs && git commit -m 'Deploy' && git push origin main"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "parcel-plugin-html-externals": "^0.2.0",
    "tap-spec": "^5.0.0",
    "tape": "^5.0.0"
  },
  "externals": {
    "./js/vendor/**/*": false
  },
  "dependencies": {
    "vextab": "^3.0.6"
  }
}
