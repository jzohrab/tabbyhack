const { join } = require('path')
const test = require('tape')

// const f = join(process.cwd(), 'app', 'guitarstring.js')
// const { GuitarString } = require(f)

test('stub', t => {
  t.equal(5, 5, 'sample')
  t.end()
})
